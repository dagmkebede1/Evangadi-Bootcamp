// const firstMultiplier = require("./myFrist");
// const secondMultiplier = require("./mySecond");
// const fs = require("fs");

// let firstResult = firstMultiplier.multiplier(5);
// // console.log(firstMultiplier.multiplier(5));

// let secondResult = secondMultiplier.multiplier(5);

// console.log(firstResult, secondResult);

// let result = firstMultiplier.multiplier(14);
// let result2 = secondMultiplier.multiplier(14);

// A module in JavaScript is just a file containing related code. In JavaScript, we use the import and export keywords to share and receive functionalities respectively across different modules. The export keyword is used to make a variable, function, class or object accessible to other modules.
// // "The value of 14 when passed through the myMultiplier
// // function is ( )."

// fs.writeFile(
//   "result.txt",
//   `The value of 14 when passed through the myMultiplier
// function is (${result}) `,
//   (err) => {
//     if (err) {
//       console.log(err);
//     }
//   }
// );

// fs.appendFile(
//   "result.txt",
//   `The value of 14 when passed through the myMultiplier
// function is (${result2}) `,
//   (err) => {
//     if (err) {
//       console.log(err);
//     }
//   }
// );
